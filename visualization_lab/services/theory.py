"""
算法理论说明：每个算法的原理 + 为什么 work 的数学推导 / 直觉证明
供前端"算法原理"弹窗展示（Markdown 格式）
"""

THEORY = {
    # ==================== 分类 ====================
    'knn': """## K近邻 KNN

### 核心思想
不显式训练模型，直接"找最近的邻居投票"。待测样本的类别由离它最近的 K 个训练样本多数决定。

### 原理
给定样本 `x`，在训练集中找距离最近的 K 个点 `N_K(x)`，预测为其中出现最多的类别：

```
ŷ = argmax_c Σ_{x_i∈N_K(x)} [y_i = c]
```

距离通常用欧氏距离 `d(x, x_i) = ||x - x_i||₂`。

### 为什么 work
**局部一致性假设**：特征空间中相邻的点大概率属于同一类——这是所有"基于实例"方法成立的根本前提。当这一假设成立时：

1. 若 K=1，最近邻分类器的误差率满足 Cover-Hart 定理：当样本量 n→∞ 时，1-NN 误差 `E_1 ≤ 2E*`（E* 为贝叶斯最优误差）。也就是说，最近邻误差不会超过最优误差的两倍。
2. 随着 K 增大，估计更稳定（投票相当于对邻域内的噪声做了多数表决，等价于用 K 个样本的类别做最大后验估计）。当 n→∞ 且 K→∞、K/n→0 时，误差渐近趋于贝叶斯最优 E*。

**直觉**：K 个近邻的投票 = 对条件概率 `P(y|x)` 的局部密度估计。K 越大方差越小（更稳）但偏差越大（邻域过大可能跨越真实决策边界），K 越小越贴合局部但越容易受噪声点影响——这正是可视化里可调节 K 观察到的权衡。

### 关键点
- 非参数方法，无显式训练，但预测需 O(n) 距离计算
- 对特征尺度敏感，需标准化
- 高维空间下"最近邻"概念会退化（维度灾难）""",

    'decision_tree': """## 决策树

### 核心思想
用一系列"if-else"规则把特征空间递归切成纯度高的小块，直到每个块内的样本基本属于同一类。

### 原理
从根节点开始，每一步选择一个特征 `f` 和阈值 `t`，把当前样本集按 `x_f ≤ t` 分成左右两个子集，使**分裂后的不纯度下降最大**：

```
增益 = I(parent) - [ (n_L/n) I(left) + (n_R/n) I(right) ]
```

不纯度 `I` 常用基尼指数 `Gini = 1 - Σ p_c²` 或信息熵 `H = -Σ p_c log p_c`。递归分裂直到达到最大深度或节点纯净。

### 为什么 work
**分而治之的贪心策略**能 work 的核心在于：每次分裂都严格降低节点的不纯度（信息增益 ≥ 0），等价于**局部最大化信息增益**。

1. 从信息论看，分裂前的熵 `H(Y)` 与分裂后条件熵 `H(Y|X_f)` 之差就是互信息 `I(Y; X_f)`。决策树每一步都在挑选"与标签最相关"的特征，相当于逐步逼近标签的真实信息量。
2. 当节点完全纯净时，`H=0`，意味着该区域的标签完全确定——决策树本质是在用分段常数函数逼近真实的决策边界 `P(y=1|x)`。
3. 树的深度足够时，可以在训练集上达到 100% 准确率（完全记住每个样本），这证明了其**表示能力**足够强；配合最大深度剪枝，是在拟合能力与泛化之间做权衡。

**直觉**：想象不断把一张混乱的色块图沿着"最能把颜色分开"的线切分，切得越多每块颜色越纯，最终形成清晰的分类边界（正是可视化动画展示的过程）。""",

    'logistic_regression': """## 逻辑回归

### 核心思想
用一个线性函数 `z = w^T x + b` 通过 sigmoid 映射成概率 `σ(z) = 1/(1+e^{-z})`，输出样本属于正类的概率，并用梯度下降最大化似然。

### 原理
模型为 `P(y=1|x) = σ(w^T x + b)`。参数通过**最大似然估计（MLE）**求得，等价于最小化交叉熵损失：

```
L(w) = -(1/n) Σ [ y_i log p_i + (1-y_i) log(1-p_i) ]
```

梯度为 `∇L = (1/n) X^T (p - y)`，沿负梯度方向迭代更新 `w ← w - η ∇L`。

### 为什么 work
**凸性 + 概率解释**，这是逻辑回归最扎实的两大支柱：

1. **损失函数严格凸**：交叉熵损失关于 `w` 是凸函数（Hessian 半正定 `H = X^T diag(p(1-p)) X`，因为 `0 < p(1-p) ≤ 1/4`），所以梯度下降不会陷入局部最优，必收敛到全局最优解——这就是为什么动画里损失曲线单调下降、从不反弹。
2. **sigmoid 是"对数几率"的线性模型**：由 `log(p/(1-p)) = w^T x` 反解得到 `p = σ(w^T x)`。这意味着它假设**对数几率是特征的线性函数**，当这一假设近似成立时，MLE 是无偏且渐近有效的（达到 Cramér-Rao 下界）。
3. 从**判别模型**角度，逻辑回归直接建模 `P(y|x)`，比朴素贝叶斯（先建模 `P(x|y)`）在"类条件分布假设错误"时更稳健，样本需求也更少（Ng & Jordan 2002 论证了这一点）。

**直觉**：sigmoid 把任意实数压到 (0,1)，远离边界（|z| 大）时概率饱和趋近 0/1，靠近边界（z≈0）时对 z 最敏感——这恰好让模型在"不确定区域"分配最陡的梯度去修正。""",

    'svm': """## 支持向量机 SVM

### 核心思想
在所有能正确分类的超平面中，选择**间隔（margin）最大**的那一个。间隔 = 离超平面最近的样本点到超平面的距离。

### 原理
对二分类（标签 ±1），要求 `y_i(w^T x_i + b) ≥ 1`，目标是最大化间隔 `2/||w||`，等价于最小化 `(1/2)||w||²`：

```
min  (1/2)||w||²   s.t.  y_i(w^T x_i + b) ≥ 1
```

引入拉格朗日乘子 α 得到对偶问题，最终解 `w = Σ α_i y_i x_i`，只有支持向量（α_i > 0）参与决定边界。

### 为什么 work
**最大间隔等价于更好的泛化**，有严格的学习理论保证：

1. **泛化误差界**：VC 理论证明，测试误差 ≤ 训练误差 + O(√(D²/ρ²/n))，其中 ρ 是间隔，D 是数据直径。**间隔 ρ 越大，泛化界越紧**——SVM 最大化 ρ 正是最小化这个上界。直观地说，间隔越宽，对训练样本的微小扰动越不敏感，越不容易过拟合。
2. **支持向量稀疏性**：最优解只由少数几个"最难分"的样本（支持向量）决定，与样本总数无关。这体现了结构风险最小化——模型复杂度由支持向量数量而非特征维度控制。
3. **核技巧**：通过 `K(x,x') = φ(x)·φ(x')` 隐式映射到高维空间，在低维线性不可分的数据在高维可能线性可分（Cover 定理），而计算仍在原空间完成。RBF 核对应无穷维特征空间，能拟合任意复杂边界。

**直觉**：想象在两组点之间放一条"最宽的隔离带"，带宽最大意味着两侧样本被最果断地分开，决策边界离所有样本都"足够远"，因此对新样本最稳。""",

    'naive_bayes': """## 朴素贝叶斯

### 核心思想
用贝叶斯定理计算后验概率，并做"条件独立"假设：给定类别后，各特征相互独立。

### 原理
贝叶斯定理：`P(y|x) ∝ P(x|y) P(y)`。朴素假设下：

```
P(x|y) = ∏_j P(x_j | y)
```

对连续特征用高斯分布建模 `P(x_j|y) ~ N(μ_{jy}, σ_{jy}²)`（参数由训练集 MLE 估计），预测取后验最大的类别 `argmax_y P(y) ∏_j P(x_j|y)`。

### 为什么 work
**条件独立假设虽然几乎总是不成立的，但分类依然有效**——这正是它最反直觉却最实用的地方：

1. **正确决策只需要排序正确**：即使 `P(x|y)` 的绝对数值是错的（因为假设错了），只要对不同类别 y 的相对大小排序正确，argmax 的结果就正确。独立假设导致的误差在各类别间往往"同向偏移"，排序被破坏的概率低于直觉预期。
2. **参数估计方差极小**：每个特征参数 `μ, σ` 只由一类样本估计，参数量 O(特征数 × 类别数)，远少于联合分布。由偏差-方差权衡，这种"高偏差、低方差"的模型在**小样本、高维**场景下反而胜出。
3. 数学上，若各特征在各类内确实独立（或近似独立），朴素贝叶斯就是真正的最优贝叶斯分类器（达到贝叶斯误差 E*）。

**直觉**：它相当于"把每个特征单独投票，再乘起来综合"。即使个别特征给出误导性投票，多数特征的正确信号通过乘积叠加仍能占据主导——前提是特征间没有强耦合（强耦合时可用决策树/随机森林等替代）。""",

    'perceptron': """## 感知机

### 核心思想
最古老的神经网络：一个线性单元 `f(x) = sign(w^T x + b)`，每次只针对**当前分错**的样本调整权重，直到所有样本正确分类。

### 原理
初始化 `w=0`，遍历样本，若 `y_i(w^T x_i) ≤ 0`（分错）则更新：

```
w ← w + η · y_i · x_i
```

重复直到一轮内无误分类（或达到迭代上限）。等价于对 hinge 型目标做随机次梯度下降。

### 为什么 work
**感知机收敛定理**（Rosenblatt, 1958）给出了严格保证：**若数据线性可分，算法必在有限步内收敛到零误差解**。

证明思路：
1. 设存在最优解 `w*` 使 `y_i(w*·x_i) ≥ γ > 0`（γ 为最优间隔，`||w*||=1`）。每次更新后 `w` 在 `w*` 方向的投影至少增加 γ：`w*·w ← w*·w + y_i(w*·x_i) ≥ w*·w + γ`。
2. 另一方面 `||w||²` 的增长有上界：`||w||² ← ||w||² + 2y_i(w·x_i) + ||x_i||² ≤ ||w||² + R²`（因 `y_i(w·x_i)≤0`，R 为样本最大范数）。
3. 迭代 T 步后 `Tγ ≤ w*·w ≤ ||w|| ≤ R√T`，故 `T ≤ R²/γ²`——**更新次数有上界**，算法必终止。

**直觉**：每次修正都"把分界面往分错的样本方向推一点"，同时保证已分对样本的几何间隔不缩小太多。在线性可分时，这种局部贪心最终必然把两类彻底分开（可视化中可看到直线一步步"翻正"）。注意：数据线性不可分时算法会永远震荡，这是它的根本局限，也催生了 SVM（加间隔与核）。""",

    'adaboost': """## AdaBoost

### 核心思想
把多个"弱学习器"（只比随机猜略好）通过加权组合，得到任意精度的"强学习器"。每轮提高被分错样本的权重，让下一个弱学习器专注攻克难点。

### 原理
每轮 t：用带权样本训练弱分类器 `h_t`，计算其误差 `ε_t`，赋予权重 `α_t = 0.5·ln((1-ε_t)/ε_t)`，更新样本权重（错样本升权、对样本降权），最终 `H(x) = sign(Σ α_t h_t(x))`。

### 为什么 work
**等价于指数损失的前向分阶段（坐标）下降**，这是 AdaBoost 最深刻的理论解释：

1. 定义指数损失 `L = Σ_i exp(-y_i F(x_i))`，其中 `F = Σ α_t h_t`。可以证明 AdaBoost 的每轮更新恰好是**对 L 在函数空间中的贪心最小化**——每一步选择使 L 下降最多的弱学习器。
2. **训练误差界**：若每轮 `ε_t ≤ 1/2 - γ`（弱学习器比随机好），则训练误差
   `err ≤ Π_t √(1-4γ²) ≤ exp(-2γ²T)`，**随轮数 T 指数级下降到 0**。这就是"弱学习等价于强学习"的定量证明。
3. **间隔理论**：随着 T 增大，即使训练误差已为 0，间隔 `y F(x)` 仍持续增大（与 SVM 的最大间隔殊途同归），因此泛化能力继续提升而非过拟合——这解释了 AdaBoost 常有的"越迭代越好"现象。

**直觉**：每个弱学习器都是"某个视角的专家"，AdaBoost 通过不断让专家们"补位"（专攻前人做错的样本）并加权表决，把一群平庸的专家组合成顶尖团队。""",

    'random_forest': """## 随机森林

### 核心思想
训练多棵决策树，每棵树用**自助采样（bagging）+ 随机特征子集**训练，最终投票（分类）或平均（回归）。是集成学习的代表。

### 原理
1. **Bagging**：从 n 个样本有放回抽 n 个，得到约 63.2% 独特样本（其余为袋外 OOB）训练一棵树。
2. **随机特征**：每次分裂只在随机选出的 m 个特征中找最优，进一步降低树之间的相关性。
3. 重复 B 次得到森林，预测取多数票或均值。

### 为什么 work
**方差削减 + 去相关**，两个机制叠加：

1. **方差削减（bagging 的数学本质）**：若 B 棵树是独立同分布、单棵方差 `σ²`、两两相关 `ρ`，则集成预测的方差为 `ρσ² + (1-ρ)σ²/B`。当 B→∞ 方差趋近 `ρσ²`。若树完全不相关（ρ=0），方差随 B 趋于 0——这是 bagging 能降低方差的核心公式。
2. **随机特征去相关**：普通 bagging 的树之间相关性 ρ 较高（因为都用同一批强特征），随机特征子集强制每棵树用不同特征组合，把 ρ 压得更低，使方差削减更接近理论下限。
3. 决策树是高方差、低偏差模型（深度大时能完美拟合训练集），恰好是"用集成降方差"的理想基学习器。森林保留了单棵树的低偏差，又通过平均消除了大部分方差，因此几乎不额外过拟合——实际中森林几乎"怎么调都不至于太差"。

**直觉**：单棵树像一位"凭经验拍脑袋"的专家，容易记错细节；让 100 位各自用不同信息、独立拍板的专家投票，个体错误相互抵消，整体判断稳定可靠。""",

    'softmax_regression': """## Softmax 回归

### 核心思想
逻辑回归的多分类推广：用 K 个线性函数 + softmax 归一化输出每个类别的概率，最大化多类交叉熵。

### 原理
对 K 个类别，第 k 类得分 `z_k = w_k^T x`，softmax 转为概率：

```
P(y=k|x) = exp(z_k) / Σ_j exp(z_j)
```

损失为多类交叉熵 `L = -(1/n) Σ Σ [y_i=k] log P(y=k|x_i)`，梯度 `∇_{w_k} L = (1/n) Σ (p_k - [y_i=k]) x_i`。

### 为什么 work
与逻辑回归同源，核心保证一致：

1. **凸性**：多类交叉熵 + softmax 的组合关于参数严格凸，梯度下降收敛到全局最优，损失曲线单调下降。
2. **最大熵的判别形式**：softmax 是"给定得分 z 下使熵最大"的概率分配。可以证明，用 softmax 拟合多类标签等价于在约束下最大化似然，其解是唯一的（因为对数似然严格凹）。
3. **梯度有漂亮解释**：`p_k - [y_i=k]` 即"预测概率 − 真实标签"，二者一致时梯度为零（不再更新），不一致时按差距修正——模型只在"预测错误"的方向上学习，与感知机的"错分驱动"精神一致，但用的是连续概率而非硬判决，因此平滑可微、可用梯度下降高效优化。

**直觉**：softmax 是一种"胜者通吃但留有余地"的归一化——指数放大了最高分的优势，同时又给所有类别分配正概率，从而能用交叉熵把"正确类别的概率"逐步推向 1。""",

    'mlp': """## 神经网络 MLP

### 核心思想
多层神经元堆叠 + 非线性激活函数，通过**反向传播**逐层传播误差梯度来更新全部参数，逼近任意复杂函数。

### 原理
前向传播逐层计算 `a^{(l)} = φ(W^{(l)} a^{(l-1)} + b^{(l)})`（φ 为 ReLU/tanh 等非线性激活），最后一层 softmax 输出概率。损失 L 对每层参数求梯度：

```
δ^{(l)} = (W^{(l+1)})^T δ^{(l+1)} ⊙ φ'(z^{(l)})   （误差反传）
∇_{W^{(l)}} L = δ^{(l)} (a^{(l-1)})^T
```

链式法则让梯度从输出层逐层回传，再沿负梯度更新参数。

### 为什么 work
**万能逼近定理 + 梯度下降的有效性**：

1. **万能逼近**（Cybenko / Hornik 1989）：只要有一层足够宽的隐藏层 + 非线性激活（如 sigmoid、ReLU），MLP 就能以任意精度逼近任意连续函数。这保证了"模型足够强，能表达真实映射"。
2. **深度带来表示优势**：深而窄的网络比浅而宽的网络能用指数级更少的神经元表达同一函数（如某些布尔函数、分层组合函数），这就是"深度学习"的表示论动机。
3. **反向传播 = 高效计算全部梯度**：若用数值微分逐个参数估计梯度需 O(W²) 次前向，反向传播只需一次前向 + 一次后向，共 O(W)，使百万级参数可训练——这是神经网络能落地的关键工程突破。
4. 损失通常非凸（有大量局部极小/鞍点），但实践与理论（如 NTK、Loss Landscape 研究）表明：随机梯度下降在高维非凸问题中往往能收敛到**泛化良好的平坦极小值**，这正是可视化里损失"快速下降后趋平"的原因。

**直觉**：每一层都在学习"更抽象的特征表示"，多层组合能表示深层非线性结构；反向传播则像"从错误反推每个神经元该负多少责任"逐层追责，最终全局调优。""",

    # ==================== 聚类 ====================
    'kmeans': """## K-Means

### 核心思想
把样本分成 K 个簇，每簇由其**质心（均值）**代表。反复执行"分配→更新质心"两步，直到簇不再变化。

### 原理
目标是最小化**簇内平方误差和（惯性）**：

```
J = Σ_{i} Σ_{k} [i∈C_k] ||x_i - μ_k||²
```

交替优化：① 固定质心，把每个点分给最近的质心（分配步）；② 固定分配，每簇质心取该簇样本均值（更新步）。

### 为什么 work
**每一步都使目标函数单调不增**，因此收敛有保证：

1. **分配步最优**：固定 `μ_k` 时，把每个 `x_i` 分给最近质心，恰好使该样本对 J 的贡献最小，J 不增。
2. **更新步最优**：固定分配后，J 关于 `μ_k` 是凸二次函数，令梯度为零得 `μ_k = (1/|C_k|) Σ_{i∈C_k} x_i`（簇内均值）恰是全局最小点，J 严格不增。
3. 综上 J 单调下降且有下界（≥0），故**必收敛**（有限种划分，最终进入不动点）。这就是动画里质心不断移动、簇越来越紧凑的数学来源。

**局限**：J 非凸，算法收敛到**局部最优**而非全局最优，结果依赖初始质心——所以才有 K-Means++（让初始质心尽量分散）等改进。这也是为什么本实验台把初始质心随机撒点，以展示"不同起点收敛到不同局部解"。

**直觉**：像 K 个服务员各自"认领"最近的顾客，然后各自站到所服务顾客的中心，顾客再重新选择最近的服务员……反复博弈最终稳定。""",

    'kmedoids': """## K-Medoids（PAM）

### 核心思想
K-Means 的变体：簇中心必须是**真实存在的样本点**（称为 medoid），而非虚拟的均值点，因此对离群点更鲁棒。

### 原理
最小化绝对距离目标（类 L1）：

```
J = Σ_i Σ_k [i∈C_k] ||x_i - m_k||
```

交替：① 分配每个点到最近 medoid；② 更新时，对每个簇尝试用簇内其他点替换当前 medoid，若总代价下降则替换（交换步）。

### 为什么 work
与 K-Means 同理，**目标函数单调下降保证收敛**：

1. 分配步固定 medoid，最近分配使 J 不增。
2. 交换步只在"代价降低"时才接受替换，J 严格不增。
3. J 有下界且状态空间（medoid 的有限选择）有限，故必收敛。

**鲁棒性来源**：K-Means 用均值 `mean`，一个极端离群点就能把质心"拉偏"；K-Medoids 用中位点（medoid）作为中心，代价函数基于 L1 距离（而非平方 L2），**单个离群点的影响有界**。数学上，L1 目标的最优中心是"几何中位数"，其对离群点的 break-down point（崩溃点）高达 50%，而均值只有 0%——即任意一个离群点都能让均值失真，但要破坏中位数需要超过一半数据是离群点。

**代价**：交换步需 O(k(n-k)²) 计算，比 K-Means 的 O(kn) 慢得多，所以只适合中小规模数据。

**直觉**：用"最能代表一群人的那个人"（真实成员）做代表，而不是"一群人的平均画像"（可能谁也不像）。""",

    'dbscan': """## DBSCAN

### 核心思想
基于**密度**聚类：把"足够密集"的区域连成簇，稀疏区域的点标为噪声。不需要预先指定簇数。

### 原理
两个参数：ε（邻域半径）、minPts（成簇最少点数）。
- **核心点**：ε 邻域内点数 ≥ minPts
- **密度直达 / 密度可达**：核心点邻域内的点可达；通过核心点链式传播
- **簇**：所有密度相连的核心点及其邻域点的并集；不属任何簇的点为**噪声**

### 为什么 work
**密度连通性是等价关系的闭包**，保证簇定义良定且唯一：

1. 核心点之间的"密度可达"关系在核心点集上构成等价关系（自反、对称、传递）。DBSCAN 的每个簇就是一个**连通分量**——就像图中找连通块，结果唯一、与扫描顺序无关（算法层面用 BFS/DFS 从核心点扩张，等价于求连通分量）。
2. **密度估计的直觉**：`ε 邻域内 ≥ minPts` 等价于局部密度 ≥ `minPts / (ε 邻域体积)`。DBSCAN 把"密度超过阈值且相互连通的区域"连成簇，这是非参数密度聚类的离散版本。
3. **抗噪**：噪声点因密度不足不会被任何簇吸收；**任意形状**：只要连通，非凸、环形簇都能识别（这是它区别于 K-Means 的关键——K-Means 只能找凸球形簇）。

**局限**：ε 大小对结果极敏感（密度差异大的数据难用单一 ε），高维下"ε 邻域"意义退化。可视化中调 ε 可直观看到簇的分裂与合并。""",

    'gmm': """## 高斯混合模型 GMM

### 核心思想
假设数据由 K 个高斯分布按权重混合生成，用**EM 算法**同时估计每个高斯的均值、协方差和混合权重，并给出每个样本属于各成分的**软概率**。

### 原理
似然：`p(x) = Σ_k π_k N(x|μ_k, Σ_k)`。直接最大化似然难以求导闭合解，故用 EM 迭代：
- **E 步**：计算后验责任度 `γ_{ik} = π_k N(x_i|μ_k,Σ_k) / Σ_j π_j N(x_i|μ_j,Σ_j)`
- **M 步**：用 γ 加权更新 `μ_k, Σ_k, π_k`（加权均值、加权协方差、责任度均值）

### 为什么 work
**EM 保证对数似然单调不减**（这是 EM 的普遍性质）：

1. 引入隐变量 z（样本来自哪个成分），用 Jensen 不等式可证：E 步构造似然的下界 Q 函数且二者在当前参数处相切；M 步最大化 Q 必使真实对数似然不降：`L(θ^{t+1}) ≥ L(θ^t)`。
2. 似然有上界（数据固定），单调上升 → 收敛到似然的局部极大值。这就是动画中对数似然曲线单调攀升的原因。
3. **比 K-Means 更一般**：K-Means 可看作 GMM 在"协方差等大且趋于 0、责任度硬分配"时的极限特例。GMM 用软概率和椭圆协方差刻画簇的**方向、大小、重叠**，能表达 K-Means 无法表示的椭圆簇和簇重叠。

**直觉**：每个高斯是"一个柔性椭球"，EM 交替做两件事——先猜每个点属于哪个椭球（E 步），再按这些"归属比例"重新调整椭球的位置、形状和大小（M 步），反复直到椭球不再移动。""",

    'hierarchical': """## 层次聚类

### 核心思想
自底向上（凝聚）把最近的簇两两合并，形成一棵**树状图（dendrogram）**，任意层级的簇数都一目了然。

### 原理
初始每个点自成一簇。反复：计算所有簇对的距离，合并距离最近的一对，直到剩下 1 簇。簇间距离由链接方式定义：
- **single**：两簇中最近点距离
- **complete**：两簇中最远点距离
- **average**：两簇所有点对平均距离

利用 **Lance-Williams 公式**可 O(1) 增量更新合并后新簇到其他簇的距离，避免每轮重算。

### 为什么 work
**贪心合并 = 逐步构建最小生成树 / 最优划分**，有结构保证：

1. **single-link 等价于 Kruskal 最小生成树**：single-link 的合并顺序恰是按距离从小到大加边构建 MST 的过程。因此 single-link 有明确的理论意义——它把聚类转化为图连通问题，能发现任意形状（链式）簇。
2. **complete / average-link 定义良定**：它们用不同的"簇间距离"度量，本质是不同范数下的凝聚贪心。每次合并都选取"当前最相似"的两簇，逐步减小簇内异质性。
3. **dendrogram 是唯一且完整的层次结构**：它记录了所有尺度下的聚类结果，切不同高度即可得到不同簇数——无需重新训练，这是它相比 K-Means 的独特优势。

**局限**：贪心不保证全局最优；single-link 对链式噪声敏感（"链条效应"），complete-link 倾向紧凑球簇。可视化中切换链接方式可直观看到簇形态差异。""",

    'spectral': """## 谱聚类

### 核心思想
不直接在原始空间聚类，而是先建样本间的相似度图，把聚类转化为**图的最优割**问题，再用图的拉普拉斯矩阵的特征向量（谱）做 K-Means。

### 原理
1. 建相似度矩阵 `W_ij = exp(-||x_i-x_j||²/2γ)`（RBF 核）
2. 计算（归一化）拉普拉斯矩阵 `L = D - W`（D 为度矩阵）或其归一化形式 `L_sym = I - D^{-1/2}WD^{-1/2}`
3. 取 L 的前 K 个小特征值对应的特征向量组成新表示
4. 在新表示上跑 K-Means

### 为什么 work
**谱图理论 + 松弛**给出了深刻保证：

1. **最小割与特征向量的联系**：把数据分成两簇 A、B 的归一化割 `NCut(A,B) = cut(A,B)(1/vol(A)+1/vol(B))`。可以证明：**NCut 的松弛最优解正是 L_sym 的第二小特征值对应的特征向量**（Fiedler 向量）。所以取前 K 个小特征向量 ≈ 近似最小化 K 路归一化割。
2. **拉普拉斯谱编码了图结构**：L 的特征值个数 0 = 图的连通分量数；若图由 K 个理想簇构成（簇间无边），则 L 有 K 个零特征值，对应特征向量是各簇的指示向量——聚类变成"读特征向量"。真实数据簇间有微弱连接，特征向量就成了"软化"的簇指示，可直接用 K-Means 分开。
3. **能发现非凸/环形簇**：相似度图天然支持任意形状（只要数据在图上连通），这是 K-Means 在原始空间做不到的。

**直觉**：把点看成图的节点，相似度高则边权大。谱聚类先"让紧密相连的点在特征空间里靠拢"，把纠结的原始形状展开成好分的球形，再交给 K-Means 收尾。""",

    # ==================== 回归 ====================
    'linear_regression': """## 线性回归

### 核心思想
假设目标 y 是特征 x 的线性函数 `ŷ = w^T x + b`，用最小二乘（最小化残差平方和）估计参数。

### 原理
最小化 `L(w) = ||Xw - y||²`（含偏置）。令梯度为零得**正规方程**：

```
w* = (X^T X)^{-1} X^T y
```

或用梯度下降迭代 `w ← w - η(2/n)X^T(Xw - y)`。

### 为什么 work
**正交投影 + 高斯-马尔可夫定理**：

1. **几何解释**：`Xw` 是 y 在 X 列空间上的**正交投影**。由投影定理，残差 `y - Xw` 垂直于列空间，此时 `||y - Xw||²` 最小。这正是正规方程的几何含义。
2. **高斯-马尔可夫定理（BLUE）**：若噪声独立同分布、零均值、等方差，则最小二乘估计在所有**线性无偏**估计中**方差最小**（最佳线性无偏估计）。这是线性回归"为什么是最优的线性方法"的理论基石。
3. **极大似然视角**：若误差服从高斯分布 `ε ~ N(0, σ²)`，则最小二乘解 = 最大似然解，二者完全一致。

**局限**：只适用于线性关系（正弦数据上 R² 很低、拟合是直线——正是可视化里展示的）；当 `X^T X` 病态（特征共线）时解不稳定，需正则化（岭/Lasso）。""",

    'polynomial_regression': """## 多项式回归

### 核心思想
把原始特征 x 升维成多项式特征 `[x, x², x³, ...]`，再套线性回归。在原始空间是曲线，在升维空间仍是"线性"模型。

### 原理
构造特征 `φ(x) = [1, x, x², ..., x^d]`，拟合 `ŷ = w^T φ(x)`。对多项式特征做标准化后，用与线性回归完全相同的最小二乘 / 梯度下降求解。

### 为什么 work
**函数逼近理论**提供了保证：

1. **Weierstrass 逼近定理**：闭区间上的任意连续函数都能被多项式**一致逼近**（任意精度）。也就是说，只要阶数 d 足够高，多项式回归理论上能拟合任何连续曲线——这就是它能从直线"弯"成任意形状的数学根源。
2. **泰勒展开直觉**：任何光滑函数在局部 `f(x) ≈ f(a) + f'(a)(x-a) + (f''(a)/2!)(x-a)² + ...`，多项式特征恰好是泰勒基。保留到 d 阶 = 用 d 阶泰勒近似目标，阶数越高近似越准。
3. **保持线性模型的全部好处**：升维后仍是线性最小二乘，解有闭式、目标凸、可复现。

**过拟合风险**：阶数过高会过度弯曲、拟合噪声（Runge 现象），阶数过低又欠拟合——可视化中调 d 可直观看到"欠拟合 → 最佳 → 过拟合"的经典权衡。""",

    'ridge_regression': """## 岭回归（L2 正则）

### 核心思想
在线性回归的损失上加入权重的 L2 平方惩罚，收缩权重，缓解共线性与过拟合。

### 原理
最小化 `L(w) = ||Xw - y||² + α||w||²`。闭式解：

```
w* = (X^T X + αI)^{-1} X^T y
```

### 为什么 work
**给 (X^T X) 加对角项改善数值稳定性**，并用偏差换方差：

1. **解决病态矩阵**：当特征共线时 `X^T X` 接近奇异、条件数巨大，`(X^T X)^{-1}` 会爆炸放大噪声。加 `αI` 后 `X^T X + αI` 的所有特征值至少为 α，条件数被压制，解稳定。这是 α 扫描动画里"直线从贴合数据逐渐被压平"的数学原因——α 越大权重被收缩越狠。
2. **偏差-方差权衡**：岭回归是有偏估计（权重被压缩），但方差随 α 增大而降低。存在一个最优 α 使总误差（偏差²+方差）最小，通常优于无正则的 OLS。
3. **贝叶斯解释**：L2 正则等价于给权重一个零均值高斯先验 `w ~ N(0, σ²/α)`，岭回归解 = 该先验下的最大后验（MAP）估计。α 越大先验越强，权重越"不敢"太大。

**与 Lasso 对比**：L2 只收缩不稀疏化，权重趋于小但通常非零；L1 则会把部分权重压到恰好 0（见 Lasso）。""",

    'lasso_regression': """## Lasso 回归（L1 正则）

### 核心思想
用 L1 惩罚替代 L2，把部分权重**精确压到 0**，同时完成"回归 + 特征选择"。

### 原理
最小化 `L(w) = ||Xw - y||² + α||w||₁`（`||w||₁ = Σ|w_j|`）。因 L1 不可微，用（近端）梯度下降 / 坐标下降求解。

### 为什么 work
**L1 的几何形状天然诱导稀疏解**：

1. **几何解释**：约束 `||w||₁ ≤ t` 是"菱形"（L1 球），而 `||w||² ≤ t` 是"圆球"。最小二乘损失等高线（椭球）与 L1 菱形**在坐标轴上的尖角**相交的概率远高于与光滑圆球相交——交点在坐标轴 ⇒ 某个 `w_j = 0`。这就是稀疏性的来源。
2. **软阈值**：坐标下降中，Lasso 的单步更新是软阈值算子 `w_j = sign(ρ_j) max(|ρ_j| - α, 0)`。当某个特征的"相关性" `ρ_j` 小于 α 时，权重被精确置 0，实现特征选择。
3. **偏差-方差 + 可解释性**：稀疏解意味着"只保留少数关键特征"，在高维小样本（特征远多于样本）下能有效避免过拟合，并给出可解释的变量选择——这是 Lasso 在统计和基因组学等领域广泛使用的核心原因。

**与岭对比**：L2 收缩但几乎不置零，L1 既收缩又稀疏。可视化中调大 α 可看到拟合线被"压平"且某些特征权重归零。""",

    'decision_tree_regression': """## 决策树回归

### 核心思想
与分类决策树同构，但把"不纯度"换成**均方误差（MSE）**，叶子节点输出该区域样本目标值的均值。得到分段常数（阶梯）拟合。

### 原理
每次分裂选使 MSE 下降最大的 `(f, t)`：

```
增益 = Σ(y-ȳ)² - [ Σ_left(y-ȳ_L)² + Σ_right(y-ȳ_R)² ]
```

递归划分，叶节点预测 `ŷ = mean(y in leaf)`。

### 为什么 work
**分段常数逼近任意函数**：

1. **表示能力**：深度足够时，每个叶子可只含一个样本，预测值精确等于该样本 y，训练误差为 0——说明模型表达力足够强。这与分类树"能完美拟合训练集"同源。
2. **本质是分段常数函数**：树把 x 轴切成若干区间，每段给常数预测。任意连续函数都能被足够细的分段常数函数一致逼近（积分/阶梯逼近原理）。动画里"深度 1 是一条水平平线，逐层分裂出阶梯，最终贴合正弦波"正是这个过程的可视化。
3. **MSE 分裂的最优性**：给定一个分裂，最优的叶值就是叶内均值（MSE 对叶值求导为零即得均值），所以每个叶节点的预测是**该区域的 MSE 最优常数**，分裂增益非负，MSE 单调下降。

**特点**：无需特征标准化、天然处理非线性、可解释（规则清晰）；但易过拟合（深度大时），且预测是阶梯状不连续。""",

    'mlp_regression': """## 神经网络回归 MLP

### 核心思想
用多层神经元 + 非线性激活拟合连续函数，输出层为线性单元（无激活），最小化 MSE。

### 原理
隐藏层 `a = φ(W_1 x + b_1)`，输出层 `ŷ = W_2 a + b_2`，损失 `L = ||ŷ - y||²`。用反向传播求梯度并更新参数（与分类 MLP 相同，仅输出层和损失不同）。

### 为什么 work
**万能逼近定理的直接体现**：

1. **万能逼近**：含非线性激活（tanh/ReLU）的单隐层网络能以任意精度逼近任意连续函数（回归场景正是逼近连续映射 `y = f(x)`）。这是 MLP 能从"接近平的初始线"逐步弯成 S 形正弦波的数学保证。
2. **非线性激活是关键**：若去掉激活（或全用线性），多层网络仍只等价于一个线性模型（线性函数的复合仍是线性），无法表达正弦等非线性。激活函数引入的非线性是"弯曲能力"的来源。
3. **反向传播 + 梯度下降**：MSE 对输出层梯度为 `ŷ - y`（预测误差），逐层反传更新所有权重，使损失单调下降。目标标准化（这里把 y 标准化到零均值单位方差）能显著加速收敛。

**直觉**：隐藏层神经元像一组"可调的基函数"，反向传播同时调整基函数的形状（隐藏层权重）和组合方式（输出层权重），让这些基函数的线性组合逐步逼近目标曲线——这本质上是一种"自适应基展开"。""",

    # ==================== 降维 ====================
    'pca': """## 主成分分析 PCA

### 核心思想
找到数据**方差最大**的相互正交方向（主成分），把数据投影到前 K 个方向，实现降维的同时最大程度保留信息。

### 原理
对中心化数据 X，计算协方差矩阵 `C = (1/n) X^T X`，对其做特征值分解 `C = V Λ V^T`。取前 K 大特征值对应的特征向量 V_K，投影 `Z = X V_K`。

### 为什么 work
**方差最大化 = 最小重构误差**，由瑞利商给出最优性：

1. **方差最大化视角**：投影方向 v 上的方差为 `Var = v^T C v`。约束 `||v||=1` 下最大化 `v^T C v` 的最优解正是 C 的**最大特征值对应的特征向量**（瑞利商定理 `λ_max = max v^T C v / v^T v`）。依次取正交方向即得各主成分。
2. **最小重构误差视角**：可以证明，前 K 个主成分张成的子空间是所有 K 维子空间中**使投影重构误差 `Σ||x - P_K x||²` 最小**的（Eckart-Young 定理的特例）。两个视角殊途同归。
3. **信息保留**：投影后保留的方差占比 = `Σ_{前K} λ_i / Σ_{全部} λ_i`。特征值越大代表该方向"信息"越多，丢弃小特征值方向损失最小。

**直觉**：把一团椭圆形的数据"压扁"到它最长的轴（方差最大方向），损失的只是最"扁"的那些方向上的信息。可视化中可看到数据被旋转对齐到主轴。""",

    'lda': """## 线性判别分析 LDA

### 核心思想
有监督降维：找到投影方向，使投影后**类间距离最大、类内距离最小**（Fisher 判别准则）。

### 原理
定义类间散度 `S_b = Σ n_c (μ_c - μ)(μ_c - μ)^T` 和类内散度 `S_w = Σ Σ (x_i - μ_c)(x_i - μ_c)^T`。最大化 Fisher 准则：

```
J(w) = w^T S_b w / w^T S_w w
```

最优解是 `S_w^{-1} S_b` 的**最大特征值对应的特征向量**。

### 为什么 work
**最大化信噪比（可分性）**，有闭式最优解：

1. **广义瑞利商**：`J(w)` 是广义瑞利商，其最大值恰为 `S_w^{-1} S_b` 的最大广义特征值，对应特征向量为最优投影方向。这是有解析保证的最优。
2. **等价于最优线性分类投影**：当各类服从**同协方差的高斯分布**时，LDA 投影方向与"贝叶斯最优分类器"的决策方向一致——即 LDA 在这种假设下达到最优可分性，投影后各类尽量"抱团且分开"。
3. **判别信息保留**：与 PCA（无监督、保方差）不同，LDA 直接优化**类别可分性**，因此分类任务中 LDA 的投影通常比 PCA 更有判别力——即使某方向方差大，若它对区分类别无帮助，LDA 也不会选它。

**局限**：最多降维到 `(类别数 - 1)` 维；当类内散度矩阵奇异（样本少、特征高）时需正则化。""",

    'svd': """## 奇异值分解 SVD

### 核心思想
把任意矩阵 A 分解为 `A = U Σ V^T`（U、V 正交，Σ 对角奇异值），取前 K 大奇异值做低秩近似，实现降维。

### 原理
对数据矩阵 X（m×n）做 SVD：`X = U Σ V^T`，其中 `σ_1 ≥ σ_2 ≥ ... ≥ 0`。取前 K 个奇异值，低秩近似 `X_K = U_K Σ_K V_K^T`，降维投影 `Z = U_K Σ_K`（或 `X V_K`）。

### 为什么 work
**Eckart-Young 定理**给出全局最优低秩近似：

1. **最优性**：在所有秩 ≤ K 的矩阵中，`X_K = U_K Σ_K V_K^T` 使 `||X - X_K||_F`（Frobenius 范数）和 `||X - X_K||₂`（谱范数）都**达到最小**。也就是说 SVD 截断是"用 K 维子空间逼近数据"的理论最优解。
2. **与 PCA 的等价性**：对中心化数据，`V^T` 的列就是主成分方向（因为 `X^T X = V Σ² V^T`），奇异值平方 = 特征值。所以 SVD 是 PCA 的**数值稳定实现**（直接对 X 分解，避免显式构造可能病态的 `X^T X`）。
3. **普适性**：SVD 对任意矩阵（不要求方阵、不要求满秩）都成立，是线性代数中信息压缩的基石，广泛用于图像压缩、推荐系统、语义分析等。

**直觉**：把矩阵看成"若干秩 1 成分的加权叠加"，奇异值越大该成分越重要。保留前 K 大奇异值 = 保留最重要的 K 个成分，丢弃噪声级的微小成分。""",

    'tsne': """## t-SNE

### 核心思想
把高维数据映射到低维（通常 2D），**优先保持局部邻居结构**——相似的点在低维也要靠近。适合可视化高维数据的簇结构。

### 原理
1. 高维：把点对相似度转为条件概率 `p_{j|i} ∝ exp(-||x_i-x_j||²/2σ_i²)`，σ_i 由困惑度（perplexity）自适应确定；对称化得 `p_ij`
2. 低维：用**重尾 t 分布**（1 自由度）建模 `q_ij ∝ (1+||y_i-y_j||²)^{-1}`
3. 用梯度下降最小化 p 与 q 的 **KL 散度** `KL(P||Q) = Σ p_ij log(p_ij/q_ij)`，梯度 `∂C/∂y_i = 4Σ (p_ij - q_ij)(y_i - y_j)(1+||y_i-y_j||²)^{-1}`

### 为什么 work
**t 分布解决"拥挤问题"，KL 最小化保留局部结构**：

1. **对称 SNE 的改进**：原始 SNE 用高斯同时建模高/低维，导致低维（2D）空间"装不下"中等距离的点对——它们会挤在一起，这就是**拥挤问题**。t-SNE 改用重尾 t 分布：低维中距离较远的点对 q 下降慢，允许它们"散开"，从而在 2D 里清晰分离出簇。
2. **KL 散度的不对称性是有意为之**：`p log(p/q)` 中，当高维 p 大（相似）而低维 q 小（被拉远）时惩罚巨大——**保证相似点必须靠拢**；反之 p 小 q 大惩罚轻——**允许不相似的点自由分布**。所以 t-SNE 严格保护局部结构，对全局结构则不强求。
3. **梯度有物理解释**：可看作点间弹簧力——`(p_ij - q_ij)` 为吸引力/排斥力的净差。相似点间是吸引，不相似点间是排斥，系统迭代到力的平衡即得最终布局。

**注意**：t-SNE 是随机优化、结果随种子和困惑度变化，簇间距离无绝对意义，只适合**探索性可视化**（这也是动画里点从随机散布逐步聚成簇的原因）。""",
}
